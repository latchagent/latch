## Core
# Postgres connection string. For local dev you can use a local Postgres or Docker.
DATABASE_URL=postgresql://latch:latch@localhost:5432/latch

# Public URL of your Latch instance (used by the dashboard + webhook setup scripts).
NEXT_PUBLIC_APP_URL=http://localhost:3000

## Auth (Better Auth)
# Change this in any non-local environment.
BETTER_AUTH_SECRET=your-secret-key-change-in-production

# Base URL Better Auth uses for callbacks and cookie settings.
BETTER_AUTH_URL=http://localhost:3000

## Telegram (optional)
# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# TELEGRAM_BOT_USERNAME=your_bot_username

# OpenAI (for summarization and creating policies with natural language, NOT for enforcement)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

